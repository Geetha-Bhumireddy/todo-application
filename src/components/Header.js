import React from 'react'

// The Header component renders the header section of the application
const Header = () => {
  return (
    // A div element with a class name 'header' for styling
    <div className='header'>
      {/* The main heading of the Todo List application */}
      <h1>Todo List</h1>
    </div>
  )
}

export default Header
